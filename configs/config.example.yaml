# Example configuration for qumo-relay
# Copy this file to config.yaml and modify as needed

server:
  # Address to bind the relay server
  address: "0.0.0.0:4433"
  
  # TLS certificate files
  cert_file: "certs/server.crt"
  key_file: "certs/server.key"

relay:
  # Upstream server URL (optional)
  # If specified, this relay will connect to the upstream and relay its streams
  # Example: "https://origin.example.com:4433"
  upstream_url: ""
  
  # Number of group caches to keep in memory
  # Higher values use more memory but reduce cache misses
  # Default: 100
  group_cache_size: 100
  
  # Frame buffer size in bytes
  # Should match your typical media frame size (e.g., 1500 for network MTU)
  # Default: 1500
  frame_capacity: 1500

